<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:paddingStart="16dp"
    android:background="#ffffff"
    android:paddingEnd="16dp"
    android:layout_width="match_parent"
    android:layout_height="64dp">

    <ImageView
        android:layout_alignParentStart="true"
        android:layout_centerVertical="true"
        android:layout_margin="8dp"
        android:src="@drawable/ic_location_city_gray_64dp"
        android:id="@+id/itemPartManagerTypeImage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <TextView
        android:layout_marginStart="8dp"
        android:textColor="#000000"
        android:scrollbarSize="16sp"
        android:gravity="center|start"
        android:text="头部"
        android:layout_toEndOf="@id/itemPartManagerTypeImage"
        android:layout_alignTop="@id/itemPartManagerTypeImage"
        android:layout_alignBottom="@id/itemPartManagerTypeImage"
        android:id="@+id/itemPartManagerTypeName"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

</RelativeLayout>